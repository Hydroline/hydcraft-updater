import type { PartialOptions } from 'overlayscrollbars'

export const HYDCRAFT_SCROLLBAR_OPTIONS = {
	scrollbars: {
		autoHide: 'leave',
		autoHideDelay: 0,
		theme: 'os-theme-hydcraft',
	},
} satisfies PartialOptions
